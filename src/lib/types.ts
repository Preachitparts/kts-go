export type BookingDetails = {
  name: string;
  phone: string;
  pickup: string;
  destination: string;
  date: Date;
  seats: number;
  emergencyContact: string;
  totalAmount: number;
  ticketNumber: string;
};
